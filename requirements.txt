# Upper bounds are deliberate: v5 used open-ended ">=" pins, so a breaking
# release of Streamlit or pandas could take the deployed app down without any
# change to this repository.
streamlit>=1.49,<2.0
pandas>=2.1,<3.0
openpyxl>=3.1,<4.0
python-docx>=1.1,<2.0
python-pptx>=0.6.23,<2.0

# SPSS .sav support. Optional: wheels are unavailable on some platforms, and the
# app degrades gracefully to CSV/Excel if the import fails.
pyreadstat>=1.2,<2.0

# Dev only
pytest>=8.0,<9.0
